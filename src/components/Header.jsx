import React from "react";

const Header = (props) => {
    return (
        <div>
            <h1>Find new motivation!</h1>
            <h2>Quote generator using React and quote API.</h2>
        </div>
    );
}

export default Header;